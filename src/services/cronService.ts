// --- FRONTEND-SAFE CRON SERVICE ---
// Cron jobs reais devem rodar no backend.
// Este arquivo é apenas um placeholder para evitar erros de build no frontend.

export function startCronJobs() {
  console.log('[Cron Service] Inicializado em modo Client-Side (No-op).');
  // Em um ambiente real, aqui iniciaríamos os jobs do node-cron.
}
